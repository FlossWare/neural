# Experiment 002

Run without a build system:

    javac -d . Experiment002.java
    java -cp . flossware.neural.Experiment002

The source is intentionally self-contained so the experiment can be discarded or extracted later without creating a production dependency.
