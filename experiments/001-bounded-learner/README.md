# Experiment 001 implementation

This is deliberately disposable research code. It implements only the state and update rule needed to test Experiment 001.

The implementation is not a framework and is not an API contract. If later experiments demonstrate that these concepts matter, we will extract the durable concepts rather than preserving this implementation for its own sake.
